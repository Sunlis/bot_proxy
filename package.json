{
  "name": "bot_proxy",
  "version": "0.1.2",
  "description": "middleware to make simple discord bots a tiny bit simpler",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "dev": "nodemon"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.5.1",
    "slash-commands": "^1.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2",
    "ts-node": "^8.5.4",
    "typescript": "^3.5.3"
  }
}
